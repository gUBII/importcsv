# Directory Tree Docs

These files are generated snapshots of key directory trees.

Regenerate:
```bash
python scripts/export_directory_trees.py
```

Default limits: max_depth=3, max_entries_per_dir=200.

Output folders:
- `docs/directories/importcsv`
- `docs/directories/moofasa`
